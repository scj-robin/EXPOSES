%% File ASTATS.STY needed to format the bibliography according the Anals of
%% Stats example.
%%
%% This style should be used with the ASTATS.BST BibTeX format
%%
%% A reference looks like :
%% \bibitem[Bose, Shrikhande and Parker (1960)]{bose}%
%% {\sc Bose, R.~C.}, {\sc Shrikhande, S.~S.} and {\sc Parker, E.~T.}
%% \newblock (1960).
%% \newblock Further results on the construction of mutually orthogonal {Latin}
%% square and the falsity of {Euler's} conjecture.
%% \newblock {\em Canad. J. Math.}
%% \newblock {\bf 12} 189--203.
%%
%% Olivier NICOLE (nicole@frinra72.bitnet) Jan. 89

% New definition of LaTeX \@for loop, to allow the use of \@for with
% MLTeX french punctuation (: is active)
\def\@otherfor#1==#2\do#3{\edef\@fortmp{#2}\ifx\@fortmp\@empty \else
    \expandafter\@forloop#2,\@nil,\@nil\@@#1{#3}\fi}

% \penalty\@m added to definition of \@citex to allow a line
% break after the `,' in citations like [Jones80,Smith77]
% (Added 23 Oct 86)
%
\def\@citex[#1]#2{\if@filesw\immediate\write\@auxout{\string\citation{#2}}\fi
  \def\@citea{}\@cite{\@otherfor\@citeb==#2\do
    {\@citea\def\@citea{,\penalty\@m}\@ifundefined
       {b@\@citeb}{{\bf ?}\@warning
       {Citation `\@citeb' on page \thepage \space undefined}}%
{\csname b@\@citeb\endcsname}}}{#1}}
% \hbox removed to allow a line to break in citations (Jan. 89)

% Square braces removed arround the citation (Jan. 89)
\def\@cite#1#2{{#1\if@tempswa, #2\fi}}
 
% Call to \@biblabel removed in \item
% \hskip-leftmargin added to allow uniindentation of the fisrt line
% All blank removed from the definition (Jan. 89)
\def\@lbibitem[#1]#2{\item[~]\hskip-\leftmargin\if@filesw%
{\def\protect##1{\string##1\space}\immediate%
\write\@auxout{\string\bibcite{#2}{#1}}}\fi\ignorespaces}
%% Placement of `}' in def of \@lbibitem corrected 29 Apr 87
%% (Error found by Arthur Ogawa.)
